import { StyleSheet } from 'react-native';

export const TabStructure = StyleSheet.create({
    mainContainer: {
        flexDirection: 'row',
    },

    container: {
        marginLeft: 'auto',
        marginRight: 'auto',
    },

    button: {
        marginTop: 100,
        width: 100,
        height: 25,
    },
})